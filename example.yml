{
  "name": "enact-protocol-signing",
  "version": "1.0.0",
  "description": "Node.js TypeScript implementation for Enact Protocol tool signing and verification",
  "main": "dist/enact-signing.js",
  "types": "dist/enact-signing.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/enact-signing.js",
    "dev": "ts-node src/enact-signing.ts",
    "example": "ts-node src/enact-signing.ts example",
    "generate": "ts-node src/enact-signing.ts generate",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "keywords": [
    "enact",
    "protocol",
    "signing",
    "verification",
    "ecdsa",
    "crypto",
    "typescript"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  },
  "dependencies": {},
  "files": [
    "dist/**/*",
    "README.md"
  ]
}